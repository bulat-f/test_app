= form_for(Comment.new(post_id: @post.id)) do |f|
  = f.hidden_field :post_id
  = f.label :text
  = f.text_area :text
  = f.simple_captcha :label => "Enter numbers.." unless user_signed_in?
  = f.submit "Comment", class: "btn btn-lg btn-primary"
