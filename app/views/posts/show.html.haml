%ol.breadcrumb
  - if @post.category
    = render @post.category.path
%h1
  = @post.title 
%p
  = @post.body
- if @post.tags.any?
  = render @tags
- if @post.comments.any?
  = render @comments
= render 'commentate'
