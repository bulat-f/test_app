%h1 Posts#edit
%p Find me in app/views/posts/edit.html.haml
= form_for(@post) do |f|
  = render 'shared/error_messages', object: f.object
  = f.label :title
  = f.text_field :title
  = f.label :body
  = f.text_area :body
  = f.submit "Save post", class: "btn btn-lg btn-primary"
